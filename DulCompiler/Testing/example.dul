class B{
    a:Int
}

fun main( ){
    a = 2
    b = 1939
    f = fun f(){
        write b
        return a
    }
    w = fun wr ( param : ()->Void ){
        write param
        write param()
    }
    w(f)
}
